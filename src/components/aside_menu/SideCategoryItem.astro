---
interface Props {
    category: string;
}

const { category } = Astro.props;
---

<button
    id={category}
    class="px-3 py-1 rounded-full text-sm font-medium text-nowrap bg-zinc-800 duration-300 hover:bg-zinc-700/60"
>
    <slot />
</button>
